apiVersion: v1
kind: Secret
metadata:
  name: gitlab-oci-registry-gitops
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  url: "registry.gitlab.com/juulun/gitops/charts"
  enableOCI: "true" 
  type: "helm"
  username: "juulun"
  password: ""
  #insecure: "false" # Ignore validity of server's TLS certificate. Defaults to "false"
