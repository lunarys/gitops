apiVersion: v1
stringData:
  BW_HOST: "https://vault.bitwarden.eu"
  #BW_USERNAME: ""
  #BW_PASSWORD: ""
  
  BW_CLIENTID: ""
  BW_APIKEY: ""

  # when using the apikey login method the password is still required to unlock the vault
  # apikey just prevents being prompted for a email or 2FA key
  BW_PASSWORD: ""
kind: Secret
metadata:
  name: bitwarden-cli
  namespace: bitwarden
type: Opaque

