apiVersion: v1
kind: Secret
metadata:
  name: gitlab-oci-registry-charts
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: gitlab-oci-registry-charts
  url: "registry.gitlab.com/juulun/helm-charts/charts"
  enableOCI: "true" 
  type: "helm"
  username: "juulun"
  password: ""
  #insecure: "false" # Ignore validity of server's TLS certificate. Defaults to "false"
