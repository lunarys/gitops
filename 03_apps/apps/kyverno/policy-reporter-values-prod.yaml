ui:
  ingress:
    hosts:
      - host: kyverno.svc.elda
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts: 
          - kyverno.svc.elda
        secretName: policy-reporter-internal-cert
