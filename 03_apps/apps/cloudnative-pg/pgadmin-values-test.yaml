ingress:
  host: pgadmin.test.svc.elda

externalsecrets:
  secrets:
    pgadmin-credentials:
      commonRemoteKey: "75d1b643-8c14-447f-b904-b3a501129670"

longhornstorage:
  volumes:
    pgadmin-config:
      pvName: ""  # TODO

autoscale:
  enabled: false  # does not work currently because the default networkpolicy of keda does not include this
  preset:
    switch:
      url: "http://nodered.svc.elda/autoscaler/pgadmin"
