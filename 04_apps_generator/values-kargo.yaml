# Renders the control-plane resources: Kargo Namespace, Project, ProjectConfig,
# Warehouse and Stages. Output goes to stage/prod/_kargo/.
#
# Rendered ONCE, covering both environments -- these live in the single Kargo
# control plane, not in a cluster. `environment` is deliberately unset; setting
# it is an error.
mode: kargo
